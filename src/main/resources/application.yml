spring:
  application:
    name: apicalendario
  datasource:
    url: jdbc:postgresql://localhost:5433/calendariolaboral
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

